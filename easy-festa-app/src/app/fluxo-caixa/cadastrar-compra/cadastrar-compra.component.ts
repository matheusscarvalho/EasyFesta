import { Component, OnInit } from '@angular/core';
// import { EventoService } from './../conta.service';
// import { Evento } from './../conta.class';
/*
@Component({
  selector: 'cadastrar-compra',
  templateUrl: './cadastrar-compra.component.html',
  styleUrls: ['./cadastrar-compra.component.css']
})

export class CadastrarEventosComponent implements OnInit {

  novoEvento: Cadastro;
  constructor(private evtService: EventoService) {
    this.novoEvento = new Cadastro();
    this.novoEvento.tipo = "Cad";
  }

  ngOnInit() {}

  adicionarEvento() {

    this.cadService.addEvento(this.novoEvento).subscribe(
      data => {
        // refresh the list
        console.log('Evento Salvo com sucesso!');
      },
      error => {
        console.error('Erro ao salvar evento!!');
      }
    );
  }

}
*/
